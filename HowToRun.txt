Cómo correr
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload


Login (form-url-encoded): POST http://localhost:8000/auth/token

Usa Authorization: Bearer <token> en llamadas a /plans/*.

Endpoints clave

GET /plans (admin ve todos, usuario solo los suyos)

POST /plans

GET /plans/{id}

PUT /plans/{id}

POST /plans/{id}/estado?estado=En%20progreso (solo admin)

DELETE /plans/{id} (solo admin)